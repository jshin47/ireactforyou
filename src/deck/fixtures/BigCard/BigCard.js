import React, { PropTypes, Component } from 'react'
import ReactDOM from 'react-dom'

import {connect, Provider} from 'react-redux'
import {applyMiddleware, bindActionCreators, combineReducers, createStore, compose} from 'redux'
import * as saga from 'redux-saga'

import ReactMotion from 'react-motion'
import ReactMotionUi from 'react-motion-ui-pack'
import ReactTransitionGroup from 'react-addons-transition-group'
import {Card, CardActions, CardHeader, CardText, CardMedia, CardTitle, Paper, Popover, Toolbar, Stepper, Step, StepButton, StepContent, StepLabel} from 'material-ui'


class BigCard extends Component {

  render () {

    const c = CardText
    c.

    return (

      <Card>
        <CardHeader>
          <CardTitle>
            
          </CardTitle>
        </CardHeader>
      </Card>

    )

  }

}
